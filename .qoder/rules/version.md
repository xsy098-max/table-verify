---
trigger: always_on
---
每一次的更新迭代，都更新 task_model.py 中的 VERSION 版本号。遵循语义化版本规则：
- 主版本号（大版本）：不兼容的 API 修改，如 1.0.0 → 2.0.0
- 次版本号（功能更新）：向下兼容的功能性新增，如 1.0.0 → 1.1.0
- 修订号（小更新）：向下兼容的问题修正，如 1.0.0 → 1.0.1

每次更新迭代时，至少递增修订号。
