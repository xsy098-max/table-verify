# 对表工具 (TableVerify) - 完整开发计划

## 一、项目背景

游戏测试人员需要频繁验证游戏配置表数据是否符合策划设计表。验证过程涉及多张配置表的链式ID查找、分隔符解析、跨表映射等复杂操作，人工对表耗时且易出错。

**目标**：构建一个可视化积木拼装式对表工具，用户无需编写代码或JSON，通过GUI界面选择和配置"积木块"来组装验证流程，一键运行并生成报告。

---

## 二、整体架构

```
┌─────────────────────────────────────────────────────┐
│                 对表工具 (TableVerify)                │
│                                                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐          │
│  │ 数据源管理│  │ 步骤拼装器│  │ 运行引擎  │          │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘          │
│       └──────────────┼──────────────┘                │
│              ┌───────┴────────┐                      │
│              │  保存/加载任务  │                      │
│              └───────┬────────┘                      │
│              ┌───────┴────────┐                      │
│              │  结果报告输出   │                      │
│              └───────────────┘                       │
└─────────────────────────────────────────────────────┘
```

### 模块划分

| 模块 | 文件 | 职责 |
|------|------|------|
| 主程序入口 | `table_verify.py` | 启动GUI主窗口 |
| 表格加载器 | `table_loader.py` | 加载CSV/XLSX/XLSM文件，构建索引 |
| 积木块引擎 | `blocks.py` | 8种积木块的执行逻辑 |
| 任务模型 | `task_model.py` | 数据源、步骤、验证组的数据结构 |
| 运行引擎 | `runner.py` | 按步骤顺序执行积木块，收集结果 |
| 报告生成 | `report.py` | 生成Excel格式的对表报告 |
| GUI主界面 | `gui_main.py` | tkinter主窗口和面板布局 |
| GUI数据源面板 | `gui_datasource.py` | 数据源管理面板 |
| GUI步骤面板 | `gui_steps.py` | 验证步骤拼装面板 |
| GUI结果面板 | `gui_results.py` | 运行结果展示面板 |

---

## 三、积木块详细设计

### 3.1 积木块清单

#### 积木1：📄 取值 (ExtractValue)

**功能**：从某张已加载的表中取出一个或一组值。

**参数表单**：

| 参数 | 类型 | 说明 | 示例 |
|------|------|------|------|
| 来源表 | 下拉选择 | 从已添加的数据源中选择 | 设计表 / BattlePassV2 |
| 取值方式 | 单选 | "按范围" 或 "按条件" | - |
| 范围 | 文本框 | Excel范围格式（按范围时填写） | C7:C56 |
| 查找列 | 下拉选择 | 在哪一列查找（按条件时填写） | ID |
| 查找值 | 文本框/下拉 | 查找的目标值（按条件时填写） | 1 |
| 返回列 | 下拉选择 | 返回哪一列的值（按条件时填写） | NeedExp |
| 保存为 | 文本框 | 给取出的值起个变量名 | 期望经验 |

**输出**：一个值（字符串/数字）或一组值（列表）

---

#### 积木2：✂️ 拆分 (SplitValue)

**功能**：将一个值按分隔符拆分成数组。

**参数表单**：

| 参数 | 类型 | 说明 | 示例 |
|------|------|------|------|
| 输入 | 下拉选择 | 选择上一步骤的输出变量 | 实际经验原始 |
| 分隔符 | 单选+自定义 | `\|` / `,` / `_` / 自定义 | \| |
| 保存为 | 文本框 | 变量名 | 实际经验 |

**输出**：列表（如 `[100, 100, 200, 300, ...]`）

---

#### 积木3：🔍 查表 (Lookup)

**功能**：用一个值去另一张表中查找对应行，返回指定列的值。

**参数表单**：

| 参数 | 类型 | 说明 | 示例 |
|------|------|------|------|
| 查找值 | 下拉选择 | 输入变量 | 奖励ID |
| 目标表 | 下拉选择 | 在哪张表查 | DropMain |
| 查找列 | 下拉选择 | 按哪一列匹配 | ID |
| 返回列 | 下拉选择 | 返回哪一列 | MustSubID1 |
| 保存为 | 文本框 | 变量名 | 子表ID |

**输出**：单个值

---

#### 积木4：🔄 批量查表 (BatchLookup)

**功能**：对一组值（数组），逐个去另一张表查找，返回一组结果。

**参数表单**：

| 参数 | 类型 | 说明 | 示例 |
|------|------|------|------|
| 输入数组 | 下拉选择 | 上一步骤的数组变量 | 奖励ID数组 |
| 目标表 | 下拉选择 | 在哪张表查 | DropMain |
| 查找列 | 下拉选择 | 按哪一列匹配 | ID |
| 返回列 | 下拉选择 | 返回哪一列 | MustSubID1 |
| 保存为 | 文本框 | 变量名 | 子表ID数组 |

**输出**：列表（与输入数组等长，一一对应）

---

#### 积木5：📦 解析掉落 (ParseDrop)

**功能**：解析 `权重,道具ID,最小值,最大值` 格式的掉落数据。

**参数表单**：

| 参数 | 类型 | 说明 | 示例 |
|------|------|------|------|
| 输入 | 下拉选择 | 上一步骤的变量 | 掉落数据数组 |
| 输入模式 | 单选 | "单个值" 或 "数组" | 数组 |
| 解析模式 | 单选 | "只取必掉（权重10000）" / "全部列出" | 只取必掉 |
| 保存为 | 文本框 | 变量名 | 实际道具列表 |

**输出格式**：`[{item_id: 1, min: 500, max: 500, weight: 10000}, ...]`

---

#### 积木6：🔗 链式查找 (ChainLookup)

**功能**：一次配置多步跨表查找，简化重复的查表操作。

**参数表单**：

| 参数 | 类型 | 说明 | 示例 |
|------|------|------|------|
| 输入 | 下拉选择 | 起始变量 | 奖励ID数组 |
| 查找链 | 列表编辑器 | 多步查找配置 | 见下方 |
| 保存为 | 文本框 | 变量名 | 最终道具 |

**查找链示例**（战令奖励的完整链路）：

```
第1跳: DropMain表 → 查找列ID → 返回列MustSubID1
第2跳: DropSub表 → 查找列ID → 返回列DropItems
```

---

#### 积木7：⚖️ 比较 (Compare)

**功能**：比较期望值和实际值，输出通过/不通过的结果。

**参数表单**：

| 参数 | 类型 | 说明 | 示例 |
|------|------|------|------|
| 期望值 | 下拉选择 | 设计表的变量 | 期望金币 |
| 实际值 | 下拉选择 | 配置表解析后的变量 | 实际金币 |
| 比较方式 | 单选 | 见下方选项 | - |
| 行标签 | 下拉选择 | 用于标识每行的标签变量 | 期望等级 |

**比较方式选项**：
- `逐项精确匹配`：数组中每个元素一一比较
- `包含匹配`：实际值中是否包含期望值
- `集合匹配`：忽略顺序，比较两个集合是否相同
- `空值跳过`：期望值为空时跳过该项（设计表中该格为空代表无奖励）

---

#### 积木8：📋 道具映射比较 (ItemMappingCompare)

**功能**：当设计表有多列道具（如金币/能量/抽卡券/天赋币/其他），需要一次性与配置表解析出的道具列表做全量比较。避免为每种道具各配一套比较流程。

**参数表单**：

| 参数 | 类型 | 说明 | 示例 |
|------|------|------|------|
| 掉落数据来源 | 下拉选择 | 上一步解析出的道具列表变量 | 实际道具列表 |
| 行标签来源 | 下拉选择 | 用于标识每行的变量（如等级） | 期望等级 |
| 映射关系 | 表格编辑器 | 多行配置：设计表列名 → 道具ID → 期望值变量 | 见下方 |
| 空值跳过 | 复选框 | 期望值为空时跳过该项 | 勾选 |

**映射关系配置**（表格形式，可增删行）：

| 设计表列含义 | 对应道具ID | 期望值来源变量 |
|-------------|-----------|--------------|
| 金币 | 1 | 期望金币 |
| 能量 | 2 | 期望能量 |
| B池抽卡券 | 3 | 期望抽卡券 |
| 天赋币 | 4 | 期望天赋币 |
| 其他 | 5 | 期望其他 |

**输入**：
- 掉落数据来源：一个二维结构，每级一个道具列表 `[{item_id, min, max, weight}, ...]`
- 各期望值变量：设计表的各列值数组

**输出**：多行比较结果

```
CompareDetail列表:
  {label: "1级", item_name: "金币", expected: 500, actual: 500, passed: True}
  {label: "1级", item_name: "能量", expected: None, actual: None, passed: True, skipped: True}
  {label: "2级", item_name: "金币", expected: None, actual: None, passed: True, skipped: True}
  {label: "2级", item_name: "能量", expected: 60, actual: 60, passed: True}
  ...
```

**战令奖励完整配置示例**：

```
步骤1-6:  [📄 取值] 分别取设计表 D-I 列，保存为 "期望等级"、"期望金币"..."期望其他"
步骤7:    [📄 取值] BattlePassV2 ID=1 → BasicRewardID → "奖励ID原始"
步骤8:    [✂️ 拆分] "奖励ID原始" 按 | 拆分 → "奖励ID数组"
步骤9:    [🔗 链式查找] "奖励ID数组" → DropMain(ID→MustSubID1) → DropSub(ID→DropItems) → "掉落数据原始"
步骤10:   [📦 解析掉落] "掉落数据原始" → "实际道具列表"
步骤11:   [📋 道具映射比较]
            掉落数据: "实际道具列表"
            行标签: "期望等级"
            映射: 金币→1→期望金币, 能量→2→期望能量, 抽卡券→3→期望抽卡券, 天赋币→4→期望天赋币, 其他→5→期望其他
            ☑ 空值跳过
```

---

### 3.2 积木块间的数据流

```
变量池（全局）
  ├── 期望经验: [100, 100, 200, ...]        ← 由"取值"积木产生
  ├── 实际经验原始: "100|100|200|..."         ← 由"取值"积木产生
  ├── 实际经验: [100, 100, 200, ...]          ← 由"拆分"积木产生
  ├── 奖励ID数组: [93000101, 93000102, ...]   ← 由"拆分"积木产生
  ├── 子表ID数组: [930001011, 930001021, ...] ← 由"批量查表"积木产生
  └── ...
```

每个积木块执行后，将结果以"变量名→值"的形式存入变量池，后续积木块可以从变量池中读取。

---

## 四、GUI界面详细设计

### 4.1 主窗口布局

```
┌──────────────────────────────────────────────────────────────┐
│  🎮 对表工具 TableVerify                    [保存任务] [加载任务]│
├───────────────────┬──────────────────────────────────────────┤
│                   │                                          │
│  📂 数据源管理     │  🔧 验证步骤                              │
│  ┌─────────────┐  │  ┌────────────────────────────────────┐  │
│  │ [+添加数据源]│  │  │ 验证组: [战令经验值 ▼]  [+新建组]   │  │
│  └─────────────┘  │  │                                    │  │
│  ┌─────────────┐  │  │ ┌──────────────────────────────┐  │  │
│  │ 📋 设计表    │  │  │ │ 步骤1: 📄 取值               │  │  │
│  │ 产消-繁体... │  │  │ │ 表:[设计表▼] 范围:[C7:C56]    │  │  │
│  │ CSV / 战令.. │  │  │ │ 保存为:[期望经验]  [▲][▼][✕]  │  │  │
│  │    [编辑][✕] │  │  │ ├──────────────────────────────┤  │  │
│  ├─────────────┤  │  │ │ 步骤2: 📄 取值               │  │  │
│  │ 📋 战令配置  │  │  │ │ 表:[BattlePassV2▼]            │  │  │
│  │ BattlePas.. │  │  │ │ 查找: ID=[1] 返回:[NeedExp]   │  │  │
│  │ XLSX / BPV2 │  │  │ │ 保存为:[实际经验原始] [▲][▼][✕]│  │  │
│  │    [编辑][✕] │  │  │ ├──────────────────────────────┤  │  │
│  ├─────────────┤  │  │ │ 步骤3: ✂️ 拆分               │  │  │
│  │ 📋 掉落主表  │  │  │ │ 输入:[实际经验原始▼] 分隔:[\|] │  │  │
│  │ DropMain..  │  │  │ │ 保存为:[实际经验]   [▲][▼][✕]  │  │  │
│  │ XLSX / Drop │  │  │ ├──────────────────────────────┤  │  │
│  │    [编辑][✕] │  │  │ │ 步骤4: ⚖️ 比较               │  │  │
│  ├─────────────┤  │  │ │ 期望:[期望经验▼]              │  │  │
│  │ ...更多表    │  │  │ │ 实际:[实际经验▼]              │  │  │
│  └─────────────┘  │  │ │ 方式:[逐项精确匹配▼] [▲][▼][✕] │  │  │
│                   │  │ └──────────────────────────────┘  │  │
│                   │  │                                    │  │
│                   │  │ [+ 添加步骤 ▼]                      │  │
│                   │  │  📄取值  ✂️拆分  🔍查表  🔄批量查表  │  │
│                   │  │  📦解析掉落  🔗链式查找  📋道具映射比较  ⚖️比较 │  │
│                   │  └────────────────────────────────────┘  │
│                   │                                          │
│                   │  [▶ 运行验证]  [📄 导出报告]              │
├───────────────────┴──────────────────────────────────────────┤
│  📊 运行结果                                                  │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ 验证组: 战令经验值                                      │  │
│  │ ✅ 全部通过 (50/50)                                     │  │
│  │                                                        │  │
│  │ 验证组: 战令奖励道具                                    │  │
│  │ ❌ 存在差异 (47/50)                                     │  │
│  │   第3级: 期望金币500, 实际0                             │  │
│  │   第15级: 期望天赋币2, 实际0                            │  │
│  │   第25级: 期望天赋币2, 实际0                            │  │
│  └────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────┘
```

### 4.2 窗口尺寸与布局

- 主窗口大小：1280 x 800，可缩放
- 左侧数据源面板：宽度约300px，可折叠
- 右侧分为上下两部分：上方步骤拼装（约60%高度），下方运行结果（约40%高度）
- 使用 `tkinter.ttk.PanedWindow` 实现可拖拽分割

### 4.3 数据源添加对话框

点击"添加数据源"弹出对话框：

```
┌─────────────────────────────────┐
│  添加数据源                      │
├─────────────────────────────────┤
│ 数据源名称: [战令配置表        ] │
│                                  │
│ 文件路径:  [E:\...\BattlePassV2] [浏览...]│
│                                  │
│ 文件类型:  ● Excel (.xlsx/.xlsm) │
│            ○ CSV (.csv)          │
│                                  │
│ 工作表:    [BattlePassV2    ▼]   │ (仅Excel时显示)
│                                  │
│ 表头行:    [2]  (数据从第几行开始)│
│                                  │
│          [确定]  [取消]          │
└─────────────────────────────────┘
```

---

## 五、核心引擎设计

### 5.1 表格加载器 (table_loader.py)

```python
class TableLoader:
    """加载表格并构建索引"""
    
    def load_csv(file_path, sheet_name=None, header_row=1) -> TableData
    def load_excel(file_path, sheet_name, header_row=1) -> TableData
    
class TableData:
    """一张表的数据容器"""
    name: str                    # 数据源名称
    file_path: str               # 文件路径
    headers: list[str]           # 列名列表
    rows: list[dict]             # 行数据，每行是一个 {列名: 值} 的字典
    index: dict[str, dict]       # 索引 {列名: {值: 行数据}}
    
    def get_range(range_str) -> list           # 按Excel范围取值，如 "C7:C56"
    def find_row(column, value) -> dict|None   # 按列值查找行
    def get_column_values(column) -> list      # 取整列值
```

**索引策略**：加载表时自动对所有列建立Hash索引，后续查表操作 O(1) 时间复杂度。

### 5.2 变量池 (variable_pool.py)

```python
class VariablePool:
    """存储积木块之间传递的中间变量"""
    
    variables: dict[str, any]    # {变量名: 值}
    
    def set(name, value)
    def get(name) -> any
    def list_names() -> list[str]  # 列出所有可用变量名（供下拉选择）
```

### 5.3 积木块执行器 (blocks.py)

```python
class Block(ABC):
    """积木块基类"""
    block_type: str              # 积木类型标识
    params: dict                 # 参数配置
    
    @abstractmethod
    def execute(variable_pool, data_sources) -> any

class ExtractValueBlock(Block):   # 📄 取值
class SplitValueBlock(Block):     # ✂️ 拆分
class LookupBlock(Block):         # 🔍 查表
class BatchLookupBlock(Block):    # 🔄 批量查表
class ParseDropBlock(Block):      # 📦 解析掉落
class ChainLookupBlock(Block):    # 🔗 链式查找
class CompareBlock(Block):        # ⚖️ 比较
class ItemMappingCompareBlock(Block):  # 📋 道具映射比较
```

### 5.4 运行引擎 (runner.py)

```python
class Runner:
    """执行验证流程"""
    
    def run(task: Task) -> RunResult
    
class Task:
    """一个完整的验证任务"""
    name: str
    data_sources: list[DataSource]
    groups: list[VerifyGroup]

class VerifyGroup:
    """一个验证组（如"战令经验值"、"战令奖励"）"""
    name: str
    steps: list[Block]

class RunResult:
    """运行结果"""
    groups: list[GroupResult]
    
class GroupResult:
    """一个验证组的结果"""
    group_name: str
    passed: int
    failed: int
    details: list[CompareDetail]   # 每一行的比较详情

class CompareDetail:
    """单行比较详情"""
    label: str          # 行标签（如"第1级"）
    expected: any       # 期望值
    actual: any         # 实际值
    passed: bool        # 是否通过
    message: str        # 说明信息
```

---

## 六、任务保存/加载设计

### 6.1 任务隔离机制

每个任务是独立的单元，拥有自己的数据源和验证步骤：

- **加载时**：只加载该任务 `.task` 文件中配置的数据源，不加载其他任务的表
- **运行时**：只执行该任务的验证组，互不干扰
- **内存管理**：切换任务时释放上一个任务的数据源

```
任务A: 战令普通验证
  ├── 数据源: 设计表CSV + BattlePassV2.xlsx + DropMain.xlsx + DropSub.xlsm + Item.xlsx
  └── 验证组: 战令经验值、战令奖励道具

任务B: 排位赛地图验证  
  ├── 数据源: 排位赛CSV + RankLevel.xlsx + GameBattleMode.xlsx
  └── 验证组: 段位地图开放情况
```

运行任务A时只加载5张表，完全不会碰任务B的3张表。

### 6.2 文件格式

任务保存为 `.task` 文件，实际是 JSON 格式（用户不需要手写，工具自动生成和读取）。

### 6.3 任务文件结构示例

```json
{
  "task_name": "战令普通验证",
  "data_sources": [
    {
      "name": "设计表",
      "file_path": "E:\\Doc\\测试相关\\配表工具\\产消-繁体-战令普通.csv",
      "file_type": "csv",
      "sheet_name": null,
      "header_row": 1
    },
    {
      "name": "BattlePassV2",
      "file_path": "E:\\Program\\Tools\\JumpXlsx\\BattlePassV2.xlsx",
      "file_type": "xlsx",
      "sheet_name": "BattlePassV2",
      "header_row": 2
    }
  ],
  "groups": [
    {
      "name": "战令经验值",
      "steps": [
        {
          "type": "extract_value",
          "params": {
            "source": "设计表",
            "mode": "range",
            "range": "C7:C56",
            "output_var": "期望经验"
          }
        },
        {
          "type": "extract_value",
          "params": {
            "source": "BattlePassV2",
            "mode": "condition",
            "find_column": "ID",
            "find_value": "1",
            "return_column": "NeedExp",
            "output_var": "实际经验原始"
          }
        },
        {
          "type": "split",
          "params": {
            "input_var": "实际经验原始",
            "delimiter": "|",
            "output_var": "实际经验"
          }
        },
        {
          "type": "compare",
          "params": {
            "expected_var": "期望经验",
            "actual_var": "实际经验",
            "mode": "exact_match",
            "label_var": null
          }
        }
      ]
    }
  ]
}
```

---

## 七、报告输出设计

### 7.1 报告格式

输出为 Excel 文件，每个验证组一个 Sheet。

### 7.2 报告示例（战令经验值 Sheet）

| 等级 | 设计表期望值 | 配置表实际值 | 结果 |
|------|------------|------------|------|
| 1级 | 100 | 100 | ✅ 通过 |
| 2级 | 100 | 100 | ✅ 通过 |
| 3级 | 200 | 200 | ✅ 通过 |
| ... | ... | ... | ... |

### 7.3 报告示例（战令奖励 Sheet）

| 等级 | 奖励类型 | 设计表期望 | 配置表实际 | 结果 | 详情 |
|------|---------|-----------|-----------|------|------|
| 1级 | 金币 | 500 | 500 | ✅ | ItemID=1 |
| 2级 | 能量 | 60 | 60 | ✅ | ItemID=xxx |
| 3级 | 金币 | 500 | 0 | ❌ | 配置表中无金币奖励 |

---

## 八、依赖和环境

### 8.1 Python依赖

```
openpyxl>=3.1.0      # 读取xlsx/xlsm文件
xlrd>=2.0.0          # 读取旧版xls文件（备选）
tkinter               # GUI（Python自带）
```

### 8.2 运行环境

- Python 3.9+
- Windows 10/11
- 无需安装额外数据库或服务

---

## 九、开发计划

### Phase 1：核心引擎（预计3个文件）

| 序号 | 任务 | 产出文件 |
|------|------|---------|
| 1.1 | 表格加载器：支持CSV和XLSX/XLSM加载，自动构建列索引 | `table_loader.py` |
| 1.2 | 变量池：存储积木块中间变量 | 包含在 `runner.py` |
| 1.3 | 7种积木块的执行逻辑 | `blocks.py` |
| 1.4 | 运行引擎：串联积木块执行 | `runner.py` |
| 1.5 | 命令行测试：用战令经验值验证流程测试引擎正确性 | 测试脚本 |

### Phase 2：GUI界面（预计4个文件）

| 序号 | 任务 | 产出文件 |
|------|------|---------|
| 2.1 | 主窗口框架：tkinter主窗口、PanedWindow布局 | `gui_main.py` |
| 2.2 | 数据源管理面板：添加/编辑/删除数据源 | `gui_datasource.py` |
| 2.3 | 验证步骤拼装面板：积木块列表、参数表单、上下移动/删除 | `gui_steps.py` |
| 2.4 | 运行结果面板：树形展示结果、统计通过率 | `gui_results.py` |

### Phase 3：保存/加载和报告

| 序号 | 任务 | 产出文件 |
|------|------|---------|
| 3.1 | 任务序列化/反序列化（JSON格式，自动处理） | 包含在 `task_model.py` |
| 3.2 | 保存/加载对话框 | 包含在 `gui_main.py` |
| 3.3 | Excel报告生成 | `report.py` |

### Phase 4：测试和优化

| 序号 | 任务 | 说明 |
|------|------|------|
| 4.1 | 战令普通完整验证测试 | 用实际数据测试经验值+奖励验证 |
| 4.2 | 排位赛地图验证测试 | 用实际数据测试排位赛地图对应 |
| 4.3 | 错误处理优化 | 文件不存在、表名错误、列名不存在等友好提示 |
| 4.4 | 性能优化 | 大表加载速度、索引优化 |

---

## 十、关键设计决策

### 10.1 表头行识别策略

游戏配置表通常有3行表头：
- 第1行：数据类型（int, string, int[]...）
- 第2行：字段名（ID, NeedExp, BasicRewardID...）
- 第3行：中文注释

**策略**：让用户指定"表头行号"（即字段名所在行），数据从下一行开始读取。默认值为2（大多数配置表字段名在第2行）。

### 10.2 分隔符处理策略

配置表中常见分隔符：
- `|`：数组元素分隔（如 `100|100|200`）
- `,`：复合数据内部分隔（如掉落 `10000,1,500,500`）
- `_`：ID组内分隔（如地图 `5_7_15`）

**策略**：每个积木块独立配置分隔符，提供常用选项+自定义输入。

### 10.3 空值处理

设计表中某些单元格可能为空（表示该等级无此奖励）。

**策略**：比较积木块提供"空值跳过"选项，设计表值为空时自动跳过该项，不报错。

### 10.4 配置表的ID查找策略

配置表中数据通常以ID为主键，但可能有多个同ID的行（如不同段位）。

**策略**：
- 默认返回第一个匹配行
- 提供高级选项：返回所有匹配行

---

## 十一、后续扩展方向

1. **多语言支持**：配置表中道具名可能是多语言ID（如 `1491127`），需要去 MultiLanguage 表查实际文本
2. **公式支持**：某些单元格可能包含Excel公式，需要计算结果而非公式文本
3. **批量任务**：一次运行多个验证任务
4. **差异高亮**：报告中用颜色标记不一致项
5. **模板市场**：内置常见验证场景的模板，一键加载
